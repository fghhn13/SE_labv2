## 2026-03-19

### 今日工作概览
- 完成地图与环境层的小步重构：引入 `Element`/注册中心、`.map` 沙盘解析、`GridMap` 全局元素查询接口，并把 `GridWorldEnvironment.step()` 改为纯物理裁判（done 来自元素交互 terminal）。
- 新增手动操控 GUI（外挂脚本）与调试脚本，便于验证终止与 `info.success` 语义。
- 整理 `docs/` 目录与新增两份地图/GUI说明文档，并更新根目录 `README.md`。

---
### 代码与架构进展
- 地图/元素系统：
  - `lab/envs/grid/elements.py`：Element 定义与 `ElementRegistry`
  - `lab/envs/grid/map_file_parser.py`：`[Metadata]` + `[Sandbox]` 的 `.map` 解析（支持空格/无空格两种行格式）
  - `lab/envs/grid/grid_map.py`：存储元素网格并提供 `get_element_at()` / `get_numeric_code_at()` 查询
  - `lab/envs/grid/maps.py`：内置地图 + 自动注册 `maps/*.map`
- 物理裁判环境：
  - `lab/envs/grid/environment.py`：`step()` 改为基于 `is_passable()` 与元素 `interact()`，Immediate Termination；`reward` 维持占位兼容
- 外挂 GUI 与调试：
  - `lab/scripts/manual_play.py`：tkinter 手动操控（固定 40px cell、底部信息表格）
  - `lab/envs/grid/debug_map_check.py` / `debug_rollout.py`：用于格式校验与最小 rollout 验证

---
### 文档更新
- 新增：
  - `docs/README.md`（文档目录导览）
  - `docs/manual_play_gui.md`（外挂 GUI 说明）
  - `docs/grid_map_module_today.md`（今天地图模块实现说明）
- 更新：
  - 根目录 `README.md`：补充 GUI 与地图/元素语义，并提示 `cost` 可能为占位值（当前 reward 已剔除）。

---
### 明日/后续建议（未实现）
- 同步更新 `docs/map_rules_and_runner.md` 以匹配“done/terminal 来自元素交互”与 reward 剔除后的新语义。
- 后续改动 `Trainer` 的 success 统计口径，使其读取 `step_result.info["success"]` 而非只依赖 `done`。

