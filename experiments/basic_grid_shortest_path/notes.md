## basic_grid_shortest_path

- 5x5 grid, start at (0, 0), goal at (4, 4), no walls.
- Agent: random baseline.
- Structure: none (NoOpStructure).

Run with:

```bash
python -m lab.cli.run_experiment lab_v2/experiments/basic_grid_shortest_path/config.json
```

